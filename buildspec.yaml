version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - mvn package
      - docker build .
#  post_build:
#    commands:
#      - echo Build completed on `date`
#      - mvn package

artifacts:
  files:
    - ./Dockerfile
    - ./irunninglog-main/target/irunninglog.jar
