version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - mvn package
      - docker build -t $IMAGE_REPO_NAME:$IMAGE_TAG .
#  post_build:
#    commands:
#      - echo Build completed on `date`
#      - mvn package

artifacts:
  files:
    - ./Dockerfile
    - ./irunninglog-main/target/irunninglog.jar
